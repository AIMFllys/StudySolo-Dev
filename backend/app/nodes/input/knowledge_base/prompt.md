知识库检索节点 — 从用户上传的文档中检索与当前学习主题相关的内容片段。

本节点不调用 LLM，而是通过向量相似度搜索（pgvector）从用户的知识库中检索最相关的文本块。

输出格式为 Markdown，包含：
- 检索到的相关片段
- 每个片段的来源信息和相关度
- 片段数量统计

使用场景：
- 用户上传了自己的学习资料（PDF/DOCX/MD），想基于自己的材料学习
- RAG（检索增强生成）模式的第一步

注意：用户需要先在知识库管理页面上传文档并完成处理后，本节点才能正常工作。
