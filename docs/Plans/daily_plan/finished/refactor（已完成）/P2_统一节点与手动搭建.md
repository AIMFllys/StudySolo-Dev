# P2 — 统一节点系统 & 手动搭建工作流

> **优先级**: 🟡 P2（P0 修复 + P1 UI 重构之后）  
> **前置依赖**: P0 ✅ (类型对齐、META 补全) · P1 ✅ (UI 审美统一)  
> **预估工时**: 20-28h  
> **目标**: 用户可以从 Node Palette 手动拖拽添加任意节点到画布，配置参数，连线，执行，验证所有 18 种节点运行正常

---

## 一、现状分析

### 1.1 当前能力矩阵

| 能力 | 状态 | 证据 |
|------|------|------|
| 从面板拖拽节点到画布 | ❌ 不存在 | WorkflowCanvas 无 `onDrop` / `onDragOver` |
| 节点面板 (Node Palette) | ❌ 不存在 | 无相关组件 |
| Store: addNode action | ❌ 不存在 | 只有 `setNodes` (全量) / `replaceWorkflowGraph` (全量) |
| 右键添加/删除节点 | ❌ 不存在 | context menu 仅作用于 sidebar 工作流列表 |
| 选中节点配置参数 | ❌ 不存在 | RightPanel 只显示输出预览，无配置表单 |
| 手动连线 | ✅ 存在 | `onConnect` handler 在 store 中 |
| 节点执行 | ✅ 存在 | `RunButton` + SSE 流式 |
| 节点渲染 | ✅ 存在 | 18 种渲染器已注册 (`RENDERER_REGISTRY`) |
| 后端节点清单 API | ✅ 存在 | `GET /api/nodes/manifest` |

### 1.2 后端节点注册表现状

18 个已注册节点，按分类:

| 分类 (Category) | 节点 | 用户手动搭建优先级 |
|-----------------|------|-------------------|
| **input** | `trigger_input`, `knowledge_base`, `web_search` | 🔴 P0 / 🟡 P1 / 🟡 P1 |
| **analysis** | `ai_analyzer`, `ai_planner`, `logic_switch`, `loop_map` | ⬜ 内部 / ⬜ 内部 / 🟢 P2 / 🟢 P2 |
| **generation** | `summary`, `outline_gen`, `content_extract`, `flashcard`, `quiz_gen`, `mind_map`, `compare`, `merge_polish` | 🔴 全部 P0 |
| **interaction** | `chat_response` | 🔴 P0 |
| **output** | `export_file`, `write_db` | 🟡 P1 |

---

## 二、Task 清单

### Phase 2A: Store 层增量操作 (2-3h)

#### T2A.1: 新增 Store Actions

**文件**: `frontend/src/stores/use-workflow-store.ts`

新增 actions:

```typescript
// 增量操作
addNode: (node: Node) => void;                           // 单节点追加
addNodes: (nodes: Node[]) => void;                       // 批量追加
deleteNode: (nodeId: string) => void;                    // 删除节点+关联边
deleteNodes: (nodeIds: string[]) => void;                // 批量删除
duplicateNode: (nodeId: string) => void;                 // 复制节点(偏移位置)

// 批量位置更新 (用于布局)
batchUpdatePositions: (positions: Record<string, {x: number, y: number}>) => void;
```

**关键细节**:
- `addNode` 需要防重复 (检查 `node.id` 是否已存在)
- `deleteNode` 需要同时删除所有与该节点关连的 edges
- `duplicateNode` 需要生成新 ID + 位置偏移 (x+50, y+50)
- 所有操作都要设置 `isDirty: true`

**验证**:
- [ ] 单元测试: addNode 后 nodes.length +1
- [ ] 单元测试: deleteNode 后关联 edges 被清除
- [ ] 单元测试: duplicateNode 生成新 ID

---

### Phase 2B: Node Palette 组件 (4-6h)

#### T2B.1: 获取节点清单

**新建文件**: `frontend/src/features/workflow/hooks/use-node-manifest.ts`

```typescript
// 从后端 /api/nodes/manifest 获取节点清单
// 使用 SWR 或 React Query 缓存
// 返回按 category 分组的节点列表
interface NodeManifestItem {
  type: string;
  category: string;
  description: string;
  is_llm_node: boolean;
  output_format: string;
  icon: string;
  color: string;
}
```

**关键决策**: 使用后端 manifest API 而非前端硬编码，确保前后端始终同步。

#### T2B.2: Node Palette 面板组件

**新建文件**: `frontend/src/features/workflow/components/panel/NodePalette.tsx`

UI 设计要点:
- 位置: 画布左侧，可折叠的垂直面板 (类似 Figma 的组件面板)
- 按 Category 分组: 📥 输入 / 🔍 分析 / ⚙️ 生成 / 💬 交互 / 📤 输出
- 每个节点项显示: icon + label + 简短描述
- 支持搜索过滤
- 可拖拽 (HTML5 Drag API: `draggable="true"` + `onDragStart`)

```
┌──────────────────┐
│ 🔍 搜索节点...    │
├──────────────────┤
│ 📥 输入           │
│  ├ ▶ 触发输入     │
│  ├ 📚 知识库检索  │
│  └ 🌐 网络搜索   │
│ ⚙️ 生成           │
│  ├ 📝 总结归纳    │
│  ├ 📋 大纲生成    │
│  ├ 📖 内容提炼    │
│  ├ 🃏 闪卡生成    │
│  ├ ❓ 测验生成    │
│  ├ 🧠 思维导图    │
│  ├ ⚖️ 对比分析    │
│  └ ✨ 合并润色    │
│ 💬 交互           │
│  └ 💬 学习回复    │
│ 📤 输出           │
│  ├ 📄 导出文件    │
│  └ 💾 写入数据    │
└──────────────────┘
```

**不在 Palette 中显示的节点**:
- `ai_analyzer` — 仅 AI 自动生成时内部使用
- `ai_planner` — 仅 AI 自动生成时内部使用

#### T2B.3: 拖拽的 DragStart 数据

```typescript
// NodePalette 中
onDragStart={(event) => {
  event.dataTransfer.setData('application/reactflow', JSON.stringify({
    type: nodeItem.type,
    label: nodeItem.label || nodeItem.description,
  }));
  event.dataTransfer.effectAllowed = 'move';
}}
```

---

### Phase 2C: WorkflowCanvas 拖放支持 (3-4h)

#### T2C.1: Canvas onDragOver + onDrop

**文件**: `frontend/src/features/workflow/components/canvas/WorkflowCanvas.tsx`

```typescript
import { useReactFlow } from '@xyflow/react';

// 在 WorkflowCanvas 内:
const { screenToFlowPosition } = useReactFlow();

const onDragOver = useCallback((event: React.DragEvent) => {
  event.preventDefault();
  event.dataTransfer.dropEffect = 'move';
}, []);

const onDrop = useCallback((event: React.DragEvent) => {
  event.preventDefault();
  
  const dataStr = event.dataTransfer.getData('application/reactflow');
  if (!dataStr) return;
  
  const { type, label } = JSON.parse(dataStr);
  
  // 将屏幕坐标转为 Flow 坐标
  const position = screenToFlowPosition({
    x: event.clientX,
    y: event.clientY,
  });
  
  // 构建新节点
  const newNode: Node = {
    id: `${type}-${Date.now()}`,
    type,
    position,
    data: {
      label: label || type,
      type,
      system_prompt: '',
      model_route: '',
      status: 'pending',
      output: '',
      output_format: 'markdown',
    },
  };
  
  addNode(newNode);
}, [screenToFlowPosition, addNode]);
```

将 `onDragOver` 和 `onDrop` 传入 `<ReactFlow>` 组件。

#### T2C.2: 拖放视觉反馈

- 拖拽进入画布时: 画布边框发光 (`ring-2 ring-primary/30`)
- 拖拽悬停时: 显示ghost节点预览
- 放置时: 节点出现 + 轻微scale动画

---

### Phase 2D: 节点右键菜单 (2-3h)

#### T2D.1: 画布节点右键菜单

**新建文件**: `frontend/src/features/workflow/components/canvas/NodeContextMenu.tsx`

菜单项:
- 🗐 复制节点
- ✏️ 编辑标签
- 🗑 删除节点
- ──────
- 📋 查看输出 (如果有)

```typescript
// WorkflowCanvas 中
const [contextMenu, setContextMenu] = useState<{
  x: number;
  y: number;
  nodeId: string;
} | null>(null);

const onNodeContextMenu = useCallback((event: React.MouseEvent, node: Node) => {
  event.preventDefault();
  setContextMenu({ x: event.clientX, y: event.clientY, nodeId: node.id });
}, []);
```

#### T2D.2: 画布空白处右键菜单

菜单项:
- 📌 在此添加节点 → 展开节点类型子菜单
- 📊 自动布局
- 🔄 全部重置

---

### Phase 2E: 节点配置面板 (4-6h)

#### T2E.1: 右侧面板 — 节点配置区域

**修改文件**: `frontend/src/components/layout/RightPanel.tsx`

在"当前焦点"区域下方，新增节点配置表单:

```
┌── 当前焦点 ──────────────────────┐
│ 📝 总结归纳          ● 待执行     │
├── 节点配置 ──────────────────────┤
│ 标签: [_总结归纳_____________]   │
│ System Prompt:                   │
│ [___________________________]    │
│ [___________________________]    │
│ [___________________________]    │
│ 模型路由: [default_______▼]     │
│ 输出格式: [markdown______▼]     │
└──────────────────────────────────┘
```

#### T2E.2: 节点类型特定配置

不同节点类型有不同的配置选项（对应后端 `node_config`）:

| 节点类型 | 可配置项 | UI 控件 |
|----------|----------|---------|
| `quiz_gen` | `types` (MCQ/填空/判断), `count`, `difficulty` | Checkbox + Number + Select |
| `export_file` | `format` (docx/pdf/md), `filename` | Select + Input |
| `web_search` | `max_results`, `search_depth` | Number + Select |
| `knowledge_base` | `top_k`, `threshold` | Number + Slider |
| `flashcard` | (无特殊配置) | — |
| `summary` | (无特殊配置，靠 system_prompt) | — |

**新建文件**: `frontend/src/features/workflow/components/panel/NodeConfigForm.tsx`

使用基于 `nodeType` 的条件渲染，为每种有特殊配置的节点显示对应的表单。

#### T2E.3: 配置数据流

```
用户修改配置 → updateNodeData(nodeId, { config: newConfig })
                 → node.data.config 更新
                 → isDirty = true
                 → debounce 保存到 IndexedDB → 保存到云
```

后端执行时通过 `node_input.node_config` 读取配置。

---

### Phase 2F: 节点执行验证 (3-4h)

#### T2F.1: 逐类型执行测试

手动搭建以下 5 条测试工作流，验证所有核心节点类型:

**Test 1: 最简流程**
```
trigger_input → summary → chat_response
```
验证: 基础 LLM 流式调用正常

**Test 2: 学习链路**
```
trigger_input → outline_gen → content_extract → flashcard
```
验证: JSON 输出 (flashcard) + 上下游数据传递

**Test 3: 测验链路**
```
trigger_input → content_extract → quiz_gen
```
验证: quiz_gen 的 `node_config` (题型/数量/难度) 是否生效

**Test 4: 对比分析**
```
trigger_input → compare
```
验证: compare 节点的输出格式 + CompareRenderer 渲染

**Test 5: 输出链路**
```
trigger_input → summary → export_file
```
验证: export_file 的配置 (format/filename) 是否生效

#### T2F.2: 错误场景测试

| 场景 | 预期行为 |
|------|----------|
| 无输入节点直接执行 | 拓扑排序报错 / 友好提示 |
| 断开的节点 | 独立执行或跳过 |
| 删除正在运行的节点 | 取消执行 + 清理状态 |
| 循环检测 | 拓扑排序报错 / 友好提示 |

---

### Phase 2G: 布局与排版 (2-3h)

#### T2G.1: Node Palette 在页面中的位置

**方案**: 在 Canvas 左侧添加可折叠的 Palette 面板

修改 workspace page 布局:

```
┌─ 侧边栏 ─┬─ Palette ─┬─── Canvas ───┬─ RightPanel ─┐
│ 工作流列表│ 节点面板  │  画布区域     │ 步骤总览     │
│           │ (可折叠)  │              │              │
└───────────┴───────────┴──────────────┴──────────────┘
```

或者作为画布内的浮动面板:

```
┌─ 侧边栏 ─┬─────── Canvas ───────┬─ RightPanel ─┐
│           │ ┌─Palette─┐         │              │
│           │ │ 节点面板 │         │              │
│           │ │ (浮动)   │         │              │
│           │ └─────────┘         │              │
└───────────┴─────────────────────┴──────────────┘
```

**推荐**: 浮动面板方案（画布内左上角，类似 XY Flow 的 Controls 组件），用按钮切换显隐。不改变现有布局结构。

#### T2G.2: 快捷键支持

| 快捷键 | 操作 |
|--------|------|
| `Delete` / `Backspace` | 删除选中节点 |
| `Ctrl+D` | 复制选中节点 |
| `Ctrl+Z` | 撤销 (Phase 3 实现) |
| `Ctrl+Shift+Z` | 重做 (Phase 3 实现) |
| `N` | 打开/关闭 Node Palette |

---

## 三、文件变更清单

| 操作 | 文件 |
|------|------|
| **新建** | `features/workflow/hooks/use-node-manifest.ts` |
| **新建** | `features/workflow/components/panel/NodePalette.tsx` |
| **新建** | `features/workflow/components/panel/NodeConfigForm.tsx` |
| **新建** | `features/workflow/components/canvas/NodeContextMenu.tsx` |
| **修改** | `stores/use-workflow-store.ts` — 新增 addNode/deleteNode/duplicateNode |
| **修改** | `features/workflow/components/canvas/WorkflowCanvas.tsx` — 添加 onDrop/onDragOver |
| **修改** | `components/layout/RightPanel.tsx` — 新增节点配置表单 |
| **修改** | `app/(dashboard)/workspace/[id]/page.tsx` — 集成 NodePalette |

---

## 四、验收标准

| # | 验收项 | 标准 |
|---|--------|------|
| A1 | 拖拽添加节点 | 从 Palette 拖拽任意节点到画布，节点正确出现 |
| A2 | 右键菜单 | 右键节点 → 可复制/删除；右键空白处 → 可添加节点 |
| A3 | 节点配置 | 选中节点 → 右侧面板显示可编辑配置 → 修改后自动保存 |
| A4 | 手动连线 | 从节点 handle 拖线到另一节点 → 边正确创建 |
| A5 | 手动执行 | 手动搭建的工作流可完整执行，SSE 流式正常 |
| A6 | Test 1-5 通过 | 5 条测试工作流全部成功执行 |
| A7 | 保存恢复 | 手动搭建 → 保存 → 刷新 → 工作流完整恢复 |
| A8 | TypeScript | `tsc --noEmit` 零错误 |
