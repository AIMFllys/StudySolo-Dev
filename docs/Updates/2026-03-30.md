<!-- 编码：UTF-8 -->

# StudySolo 2026-03-30 产品更新履历

## 【面向开发者】

### #01 workflow.css 拆分完成

将 `frontend/src/styles/workflow.css`（1945 行）拆分为 10 个模块化 CSS 文件，放入 `frontend/src/styles/workflow/` 目录：

| 文件 | 行数 | 职责 |
|------|------|------|
| `canvas-core.css` | ~130 | 画布基础（光标、控件、小地图、选区） |
| `edge-styles.css` | ~200 | 边样式（动画、铅笔风格、类型、标签） |
| `toolbar.css` | ~240 | 浮动工具栏 + 搜索栏 + emoji 选择器 |
| `canvas-overlays.css` | ~160 | 模态框 + 标注节点 |
| `context-menu.css` | ~200 | 右键菜单 + 放置面板 + 边类型面板 |
| `node-paper.css` | ~120 | 节点纸张纹理 + 画布加载器 |
| `node-skeleton.css` | ~250 | 节点骨架 + AI 生成加载器 |
| `node-handles.css` | ~70 | 手柄显隐 + 点击连线反馈 |
| `loop-nodes.css` | ~130 | 循环区域 + 循环组容器 |
| `node-result-slip.css` | ~15 | 结果卡片样式 |

所有文件均 < 300 行限制。`globals.css` 已更新为模块化导入。Next.js 构建验证通过。


---

### #02 WorkflowCanvas.tsx 拆分完成

将 `WorkflowCanvas.tsx`（918 行）拆分为 8 个模块，主文件降至 262 行：

| 新文件 | 行数 | 职责 |
|--------|------|------|
| `canvas-constants.ts` | 48 | nodeTypes, edgeTypes, BG_PRESETS 常量 |
| `canvas-node-factory.ts` | 64 | 节点数据工厂函数 |
| `HistoryControls.tsx` | 32 | 撤销/重做按钮组件 |
| `use-canvas-dnd.ts` | 66 | 拖放添加节点 |
| `use-canvas-clipboard.ts` | 93 | 复制/粘贴/删除（消除了原来的重复粘贴逻辑） |
| `use-canvas-keyboard.ts` | 85 | 键盘快捷键 |
| `use-canvas-event-listeners.ts` | 198 | 10 个 CustomEvent 监听器 |

构建验证通过，108/108 测试通过。


---

### #03 executor.py 拆分完成

将 `backend/app/engine/executor.py`（671 行）拆分为 5 个模块，主文件降至 143 行：

| 新文件 | 行数 | 职责 |
|--------|------|------|
| `topology.py` | 105 | 拓扑排序 + 分支过滤 + 等待时间辅助 |
| `node_runner.py` | 127 | 单节点执行 + 输入构建 + LLM caller 构建 |
| `loop_runner.py` | 99 | 循环容器迭代执行 |
| `level_runner.py` | 185 | 单节点流式/并行级别执行 + SSE 事件 |

同步更新了 3 个测试文件的 monkeypatch 目标以适配新模块结构。
engine 相关 24/24 测试全部通过。


---

### #04 ai_router.py 拆分完成

将 `backend/app/services/ai_router.py`（480 行）拆分为 3 个模块，主文件降至 197 行：

| 新文件 | 行数 | 职责 |
|--------|------|------|
| `llm_provider.py` | 78 | Provider 配置、client 工厂、pricing、错误记录 |
| `llm_caller.py` | 144 | 底层 LLM 调用（non-stream / stream token 生成） |

所有公开 API（`call_llm`、`call_llm_direct`、`AIRouterError` 等）的 import 路径不变。35/35 相关测试通过。


---

### #05 use-workflow-store.ts 拆分完成

将 `use-workflow-store.ts`（453 行）拆分为 Zustand slice 模式，主文件降至 185 行：

| 新文件 | 行数 | 职责 |
|--------|------|------|
| `slices/execution-slice.ts` | 129 | 执行会话 + trace 管理（6 个 action） |
| `slices/history-slice.ts` | 62 | 撤销/重做（takeSnapshot/undo/redo） |
| `workflow-store-helpers.ts` | 34 | 纯函数（deduplicateNodes, resolveSelectedNodeId, buildEdgeData） |

所有消费者的 import 路径不变。构建通过，108/108 测试通过。


---

### #06 ChatMessages.tsx 拆分完成

将 `ChatMessages.tsx`（438 行）拆分为 5 个模块，主文件降至 72 行：

| 新文件 | 行数 | 职责 |
|--------|------|------|
| `AIMessage.tsx` | 119 | AI 消息渲染 + streaming 指示器 |
| `chat-markdown-components.tsx` | 89 | Markdown 自定义渲染器 + CopyButton |
| `ChatEmptyState.tsx` | 60 | 空状态 + SkeletonLoader |
| `MagicWandLoader.tsx` | 30 | 共享 magic-wand SVG + StudySoloIcon（消除原来的重复 SVG） |

构建通过，108/108 测试通过。


---

### #07 community_node_service.py 拆分完成

将 `community_node_service.py`（428 行）按读写分离拆分，主文件降至 131 行：

| 新文件 | 行数 | 职责 |
|--------|------|------|
| `community_node_queries.py` | 161 | 序列化辅助 + 列表/详情查询 + get_node_with_prompt |

所有 import 路径通过 re-export 保持兼容。10/10 测试通过。


---

### #08 api/ai.py 拆分完成

将 `api/ai.py`（427 行）拆分为路由层 + service 层，主文件降至 64 行：
- `services/workflow_generator.py`（245 行）：图规范化 + 自动布局 + 两阶段 AI 生成管线

### #09 usage_analytics.py 拆分完成

将 `usage_analytics.py`（426 行）拆分，主文件降至 180 行：
- `usage_analytics_helpers.py`（106 行）：时间解析、bucketing、metrics 计算、数据获取

### #10 NodeStorePanel.tsx 拆分完成

将 `NodeStorePanel.tsx`（419 行）拆分为 3 个模块，主文件降至 32 行：
- `NodeStoreItem.tsx`（91 行）：节点卡片 + tooltip + 拖放
- `NodeStoreDefaultView.tsx`（107 行）：默认视图 + 分类 + 搜索 + 过滤

全部构建通过，前端 108/108 测试通过，后端相关测试通过。


---

### #12 Sidebar.tsx 拆分完成

将 `Sidebar.tsx`（405 行）拆分为 3 个模块，主文件降至 121 行：
- `SidebarActivityBar.tsx`（105 行）：Activity Bar 渲染 + 右键菜单
- `sidebar-constants.ts`（25 行）：面板配置 + 图标映射

### #15 auth/login.py 拆分完成

将 `auth/login.py`（369 行）拆分为 3 个模块，主文件降至 127 行：
- `password.py`（75 行）：密码重置路由
- `me.py`（27 行）：当前用户信息路由

### 当前进度汇总

P0（3 个）+ P1（8 个）全部完成，共拆分 13 个超标文件。
前端剩余 12 个 P2 边界超标文件（300-411 行），后端剩余 6 个（300-382 行）。
全部构建通过，前端 108/108 测试通过。
