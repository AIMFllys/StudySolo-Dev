# 2026-04-27 更新日志（全模块测试分析与 P0 引擎测试）

## 1. 全模块测试分析与规划

完成了 StudySolo 全项目的测试覆盖分析，输出详细规划文档。

- 分析范围：frontend（52 个已有测试）、backend（47 个）、5 个 Agent 子服务
- 识别出 ~85 个需新增的测试文件，按 P0-P3 四级优先级排列
- 规划文档：`docs/issues/2026-04-27-全模块测试分析与规划.md`

## 2. P0 后端引擎测试（5 个新文件，186 个测试用例）

### 新增测试文件

| 文件 | 用例数 | 覆盖模块 |
|:---|:---|:---|
| `test_topology_property.py` | 29 | 拓扑排序、环检测、分支过滤、等待时间上限 |
| `test_context_property.py` | 17 | 上下游映射构建、传递闭包、环安全 |
| `test_node_runner_property.py` | 30 | 输入构建、配置合并、LLM caller 路由、执行超时 |
| `test_level_runner_property.py` | 8 | 结果归档、错误传播、logic_switch 分支跳过 |
| `test_nodes_execute_property.py` | 102 | 节点注册完整性、13 种 LLM 节点 execute/post_process |

### 新增测试文件（第二批）

| 文件 | 用例数 | 覆盖模块 |
|:---|:---|:---|
| `test_xml_stream_parser_property.py` | 25 | XML 流解析、工具调用提取、增量喂入、标签别名 |
| `test_workflow_generator_property.py` | 21 | JSON 提取、边归一化、自动布局、布局判断 |
| `parse-sse.property.test.ts` | 10 | SSE 缓冲区解析、多事件、不完整事件 |
| `execution-state.property.test.ts` | 10 | 执行请求体构建、失败消息、中断判断 |

### 发现的 Bug

**BUG-001（High）**：`LogicSwitchNode` 和 `LoopMapNode` 的 `post_process` 调用了不存在的 `try_parse_json()` 方法，生产环境中这两个节点的后处理会崩溃。详见 `scripts/logs/test-execution-2026-04-27.md`。

涉及文件：
- `backend/app/nodes/analysis/logic_switch/node.py`
- `backend/app/nodes/analysis/loop_map/node.py`
- `backend/app/nodes/_mixins.py`（缺少 `try_parse_json` 方法）

## 3. 测试基础设施

- 安装 `pytest` 和 `pytest-asyncio` 到后端 venv
- 测试日志输出到 `scripts/logs/test-execution-2026-04-27.md`
